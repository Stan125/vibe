### This is the list of supported classes ###
supported_classes <- c("glm", "gam", "gamlss")
varimp_metrics <- c("hier.part", "relweights")
gof_metrics <- c("")
save(list = "supported_classes", file = "R/sysdata.rda")
